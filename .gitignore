# ── 환경변수 / 시크릿 — 절대 커밋 금지 ──────────────────────
.env
.env.*
!.env.aiven.example
aiven.cnf

# ── DB 덤프 파일 (용량 크고 실데이터 포함, 저장소에 안 어울림) ──
smart_factory_dump*.sql

# ── Python ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
env/
.venv/

# ── 에디터 설정 ─────────────────────────────────────────
.vscode/

# ── 개인 포트폴리오 / 면접 준비 문서 (비공개 유지) ──────────────
**/스마트팩토리_로봇모니터링_포트폴리오_면접준비.pdf
**/면접준비_최종본.pdf
# ── 로컬 테스트용 세션 쿠키 (민감정보 포함 가능) ──────────────
cookies.txt
