# ── 환경변수 / 시크릿 — 절대 커밋 금지 ──────────────────────
.env
aiven.cnf

# ── DB 덤프 파일 (용량 크고 실데이터 포함, 저장소에 안 어울림) ──
smart_factory_dump*.sql

# ── Python ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
env/
.venv/

# ── 에디터 설정 ─────────────────────────────────────────
.vscode/
